## Zero-cost Viral Moment → Merch/Digital MVP

Run locally:

```bash
npm run dev
```

Configure channels in `config/channels.yaml` (YouTube channel IDs). Storefront at `http://localhost:8787`, designs under `/designs/`.
